These are some of the dotfiles I use.

This is for personal use mostly, but you can run `./install.sh` and chose what to replace in your system if you wan't to try them out (prior backup, of course).
